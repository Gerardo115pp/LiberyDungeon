package workflows

import (
	dungeon_models "libery-dungeon-libs/models"
)

const (
	ErrUpload_TicketMismatch dungeon_models.ErrorLabel = "Upload ticket is incorrect"
	ErrMediaNotSupported     dungeon_models.ErrorLabel = "Media type not supported"
)
