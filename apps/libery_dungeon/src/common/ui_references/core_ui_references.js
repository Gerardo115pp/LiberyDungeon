import { UIReference } from "@libs/LiberyFeedback/lf_models";

export const core_dungeon_ui_references = {
    CATEGORY_CLUSTER: new UIReference("dungeon", "dungeons"),
    CATEGORY: new UIReference("cell", "cells"),
    MEDIA: new UIReference("media", "medias"),
}