import { UIReference } from "@libs/LiberyFeedback/lf_models";

export const ui_pandasworld_tag_references = {
    TAG: new UIReference("value", "values"),
    TAG_TAXONOMY: new UIReference("attribute", "attributes")
}